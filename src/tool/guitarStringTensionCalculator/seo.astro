---
import { SEORenderer } from '@jjlmoya/utils-shared';
import type { SEOSection, KnownLocale } from '../../types';

interface Props {
  locale?: KnownLocale;
  sections?: SEOSection[];
}

const { locale = 'en', sections = [] } = Astro.props;
---

<SEORenderer content={{ locale, sections }} />
