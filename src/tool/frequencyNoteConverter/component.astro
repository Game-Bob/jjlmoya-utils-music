---
import type { KnownLocale } from '../../types';
import type { FrequencyNoteConverterUI } from './ui';

interface Props {
  locale?: KnownLocale;
  ui?: Record<string, unknown>;
}

const { ui } = Astro.props;
const t = (ui ?? {}) as FrequencyNoteConverterUI;
const noteOptions = (t.noteOptions ?? 'C,C#,D,D#,E,F,F#,G,G#,A,A#,B').split('|');
---

<div
  class="fnc-root"
  id="fnc-root"
  data-note-names={t.noteNames ?? 'C,C#,D,D#,E,F,F#,G,G#,A,A#,B'}
  data-unit-hz-exact={t.unitHzExact ?? 'exact Hz'}
  data-tuned={t.tuned ?? 'Tuned'}
  data-sharp={t.sharp ?? 'sharp'}
  data-flat={t.flat ?? 'flat'}
  data-piano-octave={t.pianoOctave ?? 'Oct'}
  data-copy-feedback={t.copyFeedback ?? 'Copied!'}
  data-history-load-aria={t.historyLoadAria ?? 'Load %s Hz'}
>
  <div class="fnc__mode-switcher" role="group">
    <button class="fnc__mode-btn fnc__mode-btn--active" id="btn-hz-to-note">{t.btnHzToNote}</button>
    <button class="fnc__mode-btn" id="btn-note-to-hz">{t.btnNoteToHz}</button>
  </div>

  <div class="fnc__calibration-bar">
    <div class="fnc__cal-main">
      <span class="fnc__cal-label">{t.labelRef}</span>
      <div class="fnc__cal-presets">
        <button class="fnc__cal-preset fnc__cal-preset--active" data-ref="440">440 Hz</button>
        <button class="fnc__cal-preset" data-ref="432">432 Hz</button>
        <button class="fnc__cal-preset" data-ref="444">444 Hz</button>
      </div>
      <input
        type="number"
        id="ref-input"
        class="fnc__cal-custom"
        value="440"
        min="400"
        max="480"
        step="0.5"
        aria-label={t.ariaRefCustom}
      />
      <span class="fnc__cal-unit">Hz</span>
    </div>
    <p class="fnc__cal-info" set:html={t.calInfo} />
  </div>

  <div class="fnc__main-panel" id="panel-hz-to-note">
    <div class="fnc__input-zone">
      <label for="hz-input" class="fnc__input-label">{t.labelFrequency}</label>
      <div class="fnc__input-row">
        <input
          type="number"
          id="hz-input"
          class="fnc__big-input"
          value="440"
          min="16"
          max="20000"
          step="0.01"
          inputmode="decimal"
          aria-label={t.ariaFreqInput}
        />
        <span class="fnc__big-unit">Hz</span>
        <button class="fnc__play-btn" id="play-btn-hz" aria-label={t.ariaPlay}>
          <svg xmlns="http://www.w3.org/2000/svg" width="22" height="22" viewBox="0 0 24 24" fill="currentColor"><path d="M8 5v14l11-7z"/></svg>
        </button>
      </div>
    </div>

    <div class="fnc__result-hero" aria-live="polite">
      <div class="fnc__note-name" id="hz-result-note">A4</div>
      <div class="fnc__note-freq" id="hz-result-exact">440.00 Hz</div>
      <button class="fnc__copy-hero" id="copy-note-name" data-value="A4">
        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><rect width="14" height="14" x="8" y="8" rx="2" ry="2"/><path d="M4 16c-1.1 0-2-.9-2-2V4c0-1.1.9-2 2-2h10c1.1 0 2 .9 2 2"/></svg>
        {t.copyNoteName}
      </button>
    </div>

    <div class="fnc__tuner">
      <div class="fnc__tuner-track">
        <div class="fnc__tuner-center-mark"></div>
        <div class="fnc__tuner-needle" id="tuner-needle"></div>
      </div>
      <div class="fnc__tuner-labels">
        <span>-50</span><span>-25</span>
        <span class="fnc__tuner-label-center">0</span>
        <span>+25</span><span>+50</span>
      </div>
      <div class="fnc__cents-value" id="cents-display">0.0 cents</div>
    </div>

    <div class="fnc__harmonics">
      <div class="fnc__harmonic-card">
        <span class="fnc__harmonic-label">{t.labelOctaveDown}</span>
        <span class="fnc__harmonic-value" id="harmonic-down-val">220.00 Hz</span>
        <button class="fnc__copy-small" data-copy-target="harmonic-down-val">
          <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><rect width="14" height="14" x="8" y="8" rx="2" ry="2"/><path d="M4 16c-1.1 0-2-.9-2-2V4c0-1.1.9-2 2-2h10c1.1 0 2 .9 2 2"/></svg>
        </button>
      </div>
      <div class="fnc__harmonic-card fnc__harmonic-card--main">
        <span class="fnc__harmonic-label">{t.labelInputFreq}</span>
        <span class="fnc__harmonic-value" id="harmonic-main-val">440.00 Hz</span>
        <button class="fnc__copy-small" data-copy-target="harmonic-main-val">
          <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><rect width="14" height="14" x="8" y="8" rx="2" ry="2"/><path d="M4 16c-1.1 0-2-.9-2-2V4c0-1.1.9-2 2-2h10c1.1 0 2 .9 2 2"/></svg>
        </button>
      </div>
      <div class="fnc__harmonic-card">
        <span class="fnc__harmonic-label">{t.labelOctaveUp}</span>
        <span class="fnc__harmonic-value" id="harmonic-up-val">880.00 Hz</span>
        <button class="fnc__copy-small" data-copy-target="harmonic-up-val">
          <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><rect width="14" height="14" x="8" y="8" rx="2" ry="2"/><path d="M4 16c-1.1 0-2-.9-2-2V4c0-1.1.9-2 2-2h10c1.1 0 2 .9 2 2"/></svg>
        </button>
      </div>
    </div>
  </div>

  <div class="fnc__main-panel fnc__main-panel--hidden" id="panel-note-to-hz">
    <div class="fnc__note-selector">
      <label class="fnc__select-label" for="note-text-input">{t.labelNoteInput}</label>
      <input
        type="text"
        id="note-text-input"
        class="fnc__text-input"
        placeholder={t.placeholderNote}
      />
      <div class="fnc__note-dropdowns">
        <div class="fnc__select-group">
          <label class="fnc__select-label" for="note-select">{t.labelSetNote}</label>
          <select id="note-select" class="fnc__select">
            {noteOptions.map((opt, i) => <option value={String(i)}>{opt}</option>)}
          </select>
        </div>
        <div class="fnc__select-group">
          <label class="fnc__select-label" for="octave-select">{t.labelSetOctave}</label>
          <select id="octave-select" class="fnc__select">
            {[0,1,2,3,4,5,6,7,8].map((o) => <option value={String(o)} selected={o === 4}>{o}</option>)}
          </select>
        </div>
      </div>
    </div>

    <div class="fnc__result-hero" aria-live="polite">
      <div class="fnc__note-name" id="note-result-hz">440.00</div>
      <div class="fnc__note-freq">Hz</div>
      <button class="fnc__copy-hero" id="copy-hz-value" data-value="440.00">
        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><rect width="14" height="14" x="8" y="8" rx="2" ry="2"/><path d="M4 16c-1.1 0-2-.9-2-2V4c0-1.1.9-2 2-2h10c1.1 0 2 .9 2 2"/></svg>
        {t.copyHz}
      </button>
    </div>

    <div class="fnc__harmonics">
      <div class="fnc__harmonic-card">
        <span class="fnc__harmonic-label">{t.labelOctaveDown}</span>
        <span class="fnc__harmonic-value" id="n2h-harmonic-down">220.00 Hz</span>
        <button class="fnc__copy-small" data-copy-target="n2h-harmonic-down">
          <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><rect width="14" height="14" x="8" y="8" rx="2" ry="2"/><path d="M4 16c-1.1 0-2-.9-2-2V4c0-1.1.9-2 2-2h10c1.1 0 2 .9 2 2"/></svg>
        </button>
      </div>
      <div class="fnc__harmonic-card fnc__harmonic-card--main">
        <span class="fnc__harmonic-label">{t.labelSelectedNote}</span>
        <span class="fnc__harmonic-value" id="n2h-harmonic-main">440.00 Hz</span>
        <button class="fnc__copy-small" data-copy-target="n2h-harmonic-main">
          <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><rect width="14" height="14" x="8" y="8" rx="2" ry="2"/><path d="M4 16c-1.1 0-2-.9-2-2V4c0-1.1.9-2 2-2h10c1.1 0 2 .9 2 2"/></svg>
        </button>
      </div>
      <div class="fnc__harmonic-card">
        <span class="fnc__harmonic-label">{t.labelOctaveUp}</span>
        <span class="fnc__harmonic-value" id="n2h-harmonic-up">880.00 Hz</span>
        <button class="fnc__copy-small" data-copy-target="n2h-harmonic-up">
          <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><rect width="14" height="14" x="8" y="8" rx="2" ry="2"/><path d="M4 16c-1.1 0-2-.9-2-2V4c0-1.1.9-2 2-2h10c1.1 0 2 .9 2 2"/></svg>
        </button>
      </div>
    </div>

    <div class="fnc__play-row">
      <button class="fnc__play-full-btn" id="play-btn-note">
        <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="currentColor"><path d="M8 5v14l11-7z"/></svg>
        {t.btnListenNote}
      </button>
    </div>
  </div>

  <div class="fnc__piano-section">
    <div class="fnc__piano-label">{t.pianoLabel}</div>
    <div class="fnc__piano" id="piano" role="group" aria-label={t.pianoAria}></div>
  </div>

  <div class="fnc__history-section">
    <div class="fnc__history-header">
      <span class="fnc__history-title">{t.historyTitle}</span>
      <button class="fnc__history-clear" id="clear-history">{t.historyClear}</button>
    </div>
    <ul class="fnc__history-list" id="history-list" role="list"></ul>
    <div class="fnc__history-empty" id="history-empty">{t.historyEmpty}</div>
  </div>
</div>



<script>
  const root = document.getElementById('fnc-root') as HTMLElement;
  const ds = root?.dataset ?? {};
  const NOTE_NAMES = (ds.noteNames ?? 'C,C#,D,D#,E,F,F#,G,G#,A,A#,B').split(',');
  const UNIT_HZ_EXACT = ds.unitHzExact ?? 'exact Hz';
  const TUNED = ds.tuned ?? 'Tuned';
  const SHARP = ds.sharp ?? 'sharp';
  const FLAT = ds.flat ?? 'flat';
  const PIANO_OCTAVE = ds.pianoOctave ?? 'Oct';
  const COPY_FEEDBACK = ds.copyFeedback ?? 'Copied!';
  const HISTORY_LOAD_ARIA = ds.historyLoadAria ?? 'Load %s Hz';

  let audioCtx: AudioContext | null = null;
  let activeOscillator: OscillatorNode | null = null;
  let activeGain: GainNode | null = null;
  let currentMode: 'hz' | 'note' = 'hz';
  let referenceA4 = 440;
  let history: Array<{ hz: string; note: string }> = [];

  function getAudioContext(): AudioContext {
    if (!audioCtx) audioCtx = new AudioContext();
    return audioCtx;
  }

  function playTone(frequency: number): void {
    const ctx = getAudioContext();
    if (activeOscillator) { activeOscillator.stop(); activeOscillator = null; }
    activeGain = ctx.createGain();
    activeGain.gain.setValueAtTime(0, ctx.currentTime);
    activeGain.gain.linearRampToValueAtTime(0.4, ctx.currentTime + 0.01);
    activeGain.gain.exponentialRampToValueAtTime(0.0001, ctx.currentTime + 2);
    activeGain.connect(ctx.destination);
    activeOscillator = ctx.createOscillator();
    activeOscillator.type = 'sine';
    activeOscillator.frequency.setValueAtTime(frequency, ctx.currentTime);
    activeOscillator.connect(activeGain);
    activeOscillator.start(ctx.currentTime);
    activeOscillator.stop(ctx.currentTime + 2);
  }

  function hzToMidi(hz: number): number { return 12 * Math.log2(hz / referenceA4) + 69; }
  function midiToHz(midi: number): number { return referenceA4 * Math.pow(2, (midi - 69) / 12); }
  function midiToNoteName(midi: number): string {
    const idx = ((Math.round(midi) % 12) + 12) % 12;
    return NOTE_NAMES[idx] + (Math.floor(Math.round(midi) / 12) - 1);
  }
  function noteIndexAndOctaveToMidi(noteIndex: number, octave: number): number {
    return (octave + 1) * 12 + noteIndex;
  }

  function setElText(id: string, text: string): void {
    const el = document.getElementById(id);
    if (el) el.textContent = text;
  }

  function setHzHarmonics(hz: number, downId: string, mainId: string, upId: string): void {
    setElText(downId, `${(hz / 2).toFixed(2)} Hz`);
    setElText(mainId, `${hz.toFixed(2)} Hz`);
    setElText(upId, `${(hz * 2).toFixed(2)} Hz`);
  }

  function getNeedleClass(cents: number): string {
    if (Math.abs(cents) < 5) return 'fnc__tuner-needle--perfect';
    return cents > 0 ? 'fnc__tuner-needle--sharp' : 'fnc__tuner-needle--flat';
  }

  function getCentsLabel(cents: number): string {
    const abs = Math.abs(cents);
    if (abs < 5) return TUNED;
    const sign = cents > 0 ? '+' : '';
    const suffix = cents > 0 ? SHARP : FLAT;
    return `${sign}${cents.toFixed(1)} cents (${suffix})`;
  }

  function updateTuner(cents: number): void {
    const needle = document.getElementById('tuner-needle');
    const display = document.getElementById('cents-display');
    if (!needle || !display) return;
    const pct = Math.max(-50, Math.min(50, cents));
    needle.style.left = `${50 + (pct / 50) * 47}%`;
    ['fnc__tuner-needle--perfect', 'fnc__tuner-needle--sharp', 'fnc__tuner-needle--flat']
      .forEach((c) => needle.classList.remove(c));
    needle.classList.add(getNeedleClass(cents));
    display.textContent = getCentsLabel(cents);
  }

  function addToHistory(hz: string, note: string): void {
    if (history.find((h) => h.hz === hz)) return;
    history.unshift({ hz, note });
    if (history.length > 5) history.pop();
    renderHistory();
  }

  function renderHistory(): void {
    const list = document.getElementById('history-list');
    const empty = document.getElementById('history-empty');
    if (!list || !empty) return;
    if (history.length === 0) {
      list.innerHTML = '';
      empty.style.display = 'block';
      return;
    }
    empty.style.display = 'none';
    list.innerHTML = history
      .map(
        (h) =>
          `<li class="fnc__history-item" role="listitem"><button class="fnc__history-recall" data-hz="${h.hz}" aria-label="${HISTORY_LOAD_ARIA.replace('%s', h.hz)}"><span class="fnc__history-hz">${h.hz} Hz</span><span class="fnc__history-note">${h.note}</span></button></li>`,
      )
      .join('');
    list.querySelectorAll('.fnc__history-recall').forEach((btn) => {
      btn.addEventListener('click', () => {
        const hz = btn.getAttribute('data-hz');
        if (!hz) return;
        const inp = document.getElementById('hz-input') as HTMLInputElement | null;
        if (inp) { inp.value = hz; updateHzToNote(); }
        if (currentMode !== 'hz') switchMode('hz');
      });
    });
  }

  function updateHzToNote(): void {
    const hzInput = document.getElementById('hz-input') as HTMLInputElement | null;
    if (!hzInput) return;
    let hz = parseFloat(hzInput.value);
    if (isNaN(hz) || hz <= 0) hz = 440;
    hz = Math.max(16, Math.min(20000, hz));
    const midi = hzToMidi(hz);
    const roundedMidi = Math.round(midi);
    const cents = (midi - roundedMidi) * 100;
    const noteName = midiToNoteName(roundedMidi);
    const exactHz = midiToHz(roundedMidi);
    setElText('hz-result-note', noteName);
    setElText('hz-result-exact', `${exactHz.toFixed(2)} ${UNIT_HZ_EXACT}`);
    const copyBtn = document.getElementById('copy-note-name');
    if (copyBtn) copyBtn.setAttribute('data-value', noteName);
    setHzHarmonics(hz, 'harmonic-down-val', 'harmonic-main-val', 'harmonic-up-val');
    updateTuner(cents);
    updatePianoHighlight(((roundedMidi % 12) + 12) % 12);
    addToHistory(hz.toFixed(2), noteName);
  }

  function updateNoteToHz(fromInput = false): void {
    const noteSelect = document.getElementById('note-select') as HTMLSelectElement | null;
    const octaveSelect = document.getElementById('octave-select') as HTMLSelectElement | null;
    const noteTextInput = document.getElementById('note-text-input') as HTMLInputElement | null;
    if (!noteSelect || !octaveSelect || !noteTextInput) return;
    const noteIndex = parseInt(noteSelect.value);
    const octave = parseInt(octaveSelect.value);
    if (!fromInput) noteTextInput.value = (NOTE_NAMES[noteIndex] ?? '') + octave;
    const hz = midiToHz(noteIndexAndOctaveToMidi(noteIndex, octave));
    setElText('note-result-hz', hz.toFixed(2));
    const copyBtn = document.getElementById('copy-hz-value');
    if (copyBtn) copyBtn.setAttribute('data-value', hz.toFixed(2));
    setHzHarmonics(hz, 'n2h-harmonic-down', 'n2h-harmonic-main', 'n2h-harmonic-up');
    updatePianoHighlight(noteIndex);
  }

  function applyModifier(noteIndex: number, modifier: string | undefined): number {
    if (!modifier) return noteIndex;
    const mod = modifier.toUpperCase();
    if (mod === '#' || mod === 'SHARP') return (noteIndex + 1) % 12;
    if (mod === 'B' || mod === 'FLAT') return (noteIndex - 1 + 12) % 12;
    return noteIndex;
  }

  function parseNoteInput(input: string): { noteIndex: number; octave: number } | null {
    const clean = input.trim().toUpperCase();
    if (!clean) return null;
    const match = clean.match(/^([A-G]|DO|RE|MI|FA|SOL|LA|SI)(#|B|SHARP|FLAT)?(\d)?$/i);
    if (!match) return null;
    const enNotes = ['C', 'C#', 'D', 'D#', 'E', 'F', 'F#', 'G', 'G#', 'A', 'A#', 'B'];
    const esNotes = ['DO', 'DO#', 'RE', 'RE#', 'MI', 'FA', 'FA#', 'SOL', 'SOL#', 'LA', 'LA#', 'SI'];
    const noteStr = match[1] ?? '';
    let idx = enNotes.indexOf(noteStr);
    if (idx === -1) idx = esNotes.indexOf(noteStr);
    if (idx === -1) return null;
    const octave = match[3] ? parseInt(match[3]) : 4;
    return { noteIndex: applyModifier(idx, match[2]), octave };
  }

  function switchMode(mode: 'hz' | 'note'): void {
    currentMode = mode;
    const panelHz = document.getElementById('panel-hz-to-note');
    const panelNote = document.getElementById('panel-note-to-hz');
    const btnHz = document.getElementById('btn-hz-to-note');
    const btnNote = document.getElementById('btn-note-to-hz');
    if (!panelHz || !panelNote || !btnHz || !btnNote) return;
    if (mode === 'hz') {
      panelHz.classList.remove('fnc__main-panel--hidden');
      panelNote.classList.add('fnc__main-panel--hidden');
      btnHz.classList.add('fnc__mode-btn--active');
      btnNote.classList.remove('fnc__mode-btn--active');
    } else {
      panelNote.classList.remove('fnc__main-panel--hidden');
      panelHz.classList.add('fnc__main-panel--hidden');
      btnNote.classList.add('fnc__mode-btn--active');
      btnHz.classList.remove('fnc__mode-btn--active');
    }
  }

  function updatePianoHighlight(noteIndex: number): void {
    document.querySelectorAll('.fnc__key--active').forEach((k) => k.classList.remove('fnc__key--active'));
    document.querySelectorAll(`[data-note="${noteIndex}"]`).forEach((k) => k.classList.add('fnc__key--active'));
  }

  function buildWhiteKey(noteIdx: number, oct: number, bIdx: number): HTMLButtonElement {
    const wKey = document.createElement('button');
    wKey.className = 'fnc__key fnc__key--white';
    wKey.setAttribute('data-note', String(noteIdx));
    wKey.setAttribute('data-octave', String(oct));
    wKey.setAttribute('aria-label', `${NOTE_NAMES[noteIdx] ?? ''}${oct}`);
    const label = document.createElement('span');
    label.className = 'fnc__key-label';
    label.textContent = (NOTE_NAMES[noteIdx] ?? '').replace('#', '');
    wKey.appendChild(label);
    if (bIdx !== -1) {
      const bKey = document.createElement('button');
      bKey.className = 'fnc__key fnc__key--black';
      bKey.setAttribute('data-note', String(bIdx));
      bKey.setAttribute('data-octave', String(oct));
      bKey.setAttribute('aria-label', `${NOTE_NAMES[bIdx] ?? ''}${oct}`);
      wKey.appendChild(bKey);
    }
    return wKey;
  }

  function buildOctave(oct: number): HTMLDivElement {
    const wrap = document.createElement('div');
    wrap.className = 'fnc__piano-octave';
    wrap.setAttribute('data-octave', String(oct));
    const whitePattern = [0, 2, 4, 5, 7, 9, 11];
    const blackPattern = [1, 3, -1, 6, 8, 10, -1];
    whitePattern.forEach((noteIdx, pos) => {
      wrap.appendChild(buildWhiteKey(noteIdx, oct, blackPattern[pos] ?? -1));
    });
    const octLabel = document.createElement('div');
    octLabel.className = 'fnc__piano-octave-label';
    octLabel.textContent = `${PIANO_OCTAVE} ${oct}`;
    wrap.appendChild(octLabel);
    return wrap;
  }

  function handlePianoClick(e: Event): void {
    const target = (e.target as HTMLElement).closest('[data-note]') as HTMLElement | null;
    if (!target) return;
    const noteIdx = parseInt(target.getAttribute('data-note') ?? '0');
    const oct = parseInt(target.getAttribute('data-octave') ?? '4');
    const hz = midiToHz(noteIndexAndOctaveToMidi(noteIdx, oct));
    playTone(hz);
    const hzInput = document.getElementById('hz-input') as HTMLInputElement | null;
    if (hzInput) { hzInput.value = hz.toFixed(2); updateHzToNote(); }
    if (currentMode !== 'hz') switchMode('hz');
  }

  function buildPiano(): void {
    const piano = document.getElementById('piano');
    if (!piano) return;
    piano.innerHTML = '';
    for (let oct = 3; oct <= 5; oct++) piano.appendChild(buildOctave(oct));
    piano.addEventListener('click', handlePianoClick);
  }

  function copyText(text: string, btn: HTMLElement): void {
    const orig = btn.innerHTML;
    navigator.clipboard.writeText(text).then(() => {
      btn.classList.add('fnc__copy-small--done');
      btn.innerHTML = `<svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"></polyline></svg>`;
      setTimeout(() => { btn.classList.remove('fnc__copy-small--done'); btn.innerHTML = orig; }, 2000);
    });
  }

  function setupHeroCopy(id: string): void {
    document.getElementById(id)?.addEventListener('click', (e) => {
      const btn = e.currentTarget as HTMLElement;
      const orig = btn.innerHTML;
      const val = btn.getAttribute('data-value') ?? '';
      navigator.clipboard.writeText(val).then(() => {
        btn.classList.add('fnc__copy-hero--done');
        btn.innerHTML = `<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"></polyline></svg> ${COPY_FEEDBACK}`;
        setTimeout(() => { btn.classList.remove('fnc__copy-hero--done'); btn.innerHTML = orig; }, 2000);
      });
    });
  }

  function setupCopyHandlers(): void {
    setupHeroCopy('copy-note-name');
    setupHeroCopy('copy-hz-value');
    document.addEventListener('click', (e) => {
      const btn = (e.target as HTMLElement).closest('.fnc__copy-small') as HTMLElement | null;
      if (!btn) return;
      const targetId = btn.getAttribute('data-copy-target');
      if (!targetId) return;
      const el = document.getElementById(targetId);
      if (!el) return;
      copyText(el.textContent?.replace(' Hz', '').trim() ?? '', btn);
    });
  }

  function setupModeHandlers(): void {
    document.getElementById('btn-hz-to-note')?.addEventListener('click', () => switchMode('hz'));
    document.getElementById('btn-note-to-hz')?.addEventListener('click', () => switchMode('note'));
  }

  function setupPlayHandlers(): void {
    document.getElementById('play-btn-hz')?.addEventListener('click', () => {
      const inp = document.getElementById('hz-input') as HTMLInputElement | null;
      if (!inp) return;
      const hz = parseFloat(inp.value);
      if (!isNaN(hz) && hz > 0) playTone(hz);
    });
    document.getElementById('play-btn-note')?.addEventListener('click', () => {
      const ns = document.getElementById('note-select') as HTMLSelectElement | null;
      const os = document.getElementById('octave-select') as HTMLSelectElement | null;
      if (!ns || !os) return;
      playTone(midiToHz(noteIndexAndOctaveToMidi(parseInt(ns.value), parseInt(os.value))));
    });
  }

  function setupCalibrationHandlers(): void {
    document.getElementById('ref-input')?.addEventListener('input', (e) => {
      const val = parseFloat((e.target as HTMLInputElement).value);
      if (isNaN(val) || val <= 300 || val >= 600) return;
      referenceA4 = val;
      document.querySelectorAll('.fnc__cal-preset').forEach((p) => p.classList.remove('fnc__cal-preset--active'));
      if (currentMode === 'hz') updateHzToNote();
      else updateNoteToHz();
    });
    document.querySelectorAll('.fnc__cal-preset').forEach((btn) => {
      btn.addEventListener('click', () => {
        const ref = parseFloat(btn.getAttribute('data-ref') ?? '440');
        referenceA4 = ref;
        const refInput = document.getElementById('ref-input') as HTMLInputElement | null;
        if (refInput) refInput.value = String(ref);
        document.querySelectorAll('.fnc__cal-preset').forEach((p) => p.classList.remove('fnc__cal-preset--active'));
        btn.classList.add('fnc__cal-preset--active');
        if (currentMode === 'hz') updateHzToNote();
        else updateNoteToHz();
      });
    });
  }

  function setupInputHandlers(): void {
    document.getElementById('hz-input')?.addEventListener('input', updateHzToNote);
    document.getElementById('note-select')?.addEventListener('change', () => updateNoteToHz());
    document.getElementById('octave-select')?.addEventListener('change', () => updateNoteToHz());
    document.getElementById('note-text-input')?.addEventListener('input', (e) => {
      const val = (e.target as HTMLInputElement).value;
      const parsed = parseNoteInput(val);
      if (!parsed) return;
      const ns = document.getElementById('note-select') as HTMLSelectElement | null;
      const os = document.getElementById('octave-select') as HTMLSelectElement | null;
      if (!ns || !os) return;
      ns.value = String(parsed.noteIndex);
      os.value = String(parsed.octave);
      updateNoteToHz(true);
    });
  }

  document.addEventListener('DOMContentLoaded', () => {
    buildPiano();
    updateHzToNote();
    updateNoteToHz();
    renderHistory();
    setupInputHandlers();
    setupModeHandlers();
    setupPlayHandlers();
    setupCalibrationHandlers();
    setupCopyHandlers();
    document.getElementById('clear-history')?.addEventListener('click', () => {
      history = [];
      renderHistory();
    });
  });
</script>
